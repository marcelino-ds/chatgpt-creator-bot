# Binary output
chatgpt-creator
chatgpt-creator.exe
*.exe

# Agent instructions
agents.md
AGENTS.md

# OS files
.DS_Store
Thumbs.db

# Config and Results
results.txt
blacklist.json

# Runtime database (SQLite + WAL sidecars)
*.db
*.db-shm
*.db-wal

# Frontend toolchain
web/node_modules/
web/.vite/
npm-debug.log*

# Note: internal/webui/dist is intentionally committed so `go build`
# works without running the Node toolchain first (go:embed needs it).
